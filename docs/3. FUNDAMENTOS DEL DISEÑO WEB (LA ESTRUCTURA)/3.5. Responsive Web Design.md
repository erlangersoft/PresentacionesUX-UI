# 3.5. Responsive Web Design

## Introducción

Responsive Web Design (RWD) es el enfoque de diseño que hace que las páginas web se adapten a diferentes tamaños de pantalla y dispositivos. En 2010, Ethan Marcotte introdujo este concepto que revolucionó el diseño web.

**Pilares del Responsive Design:**
- Grillas fluidas (fluid grids)
- Imágenes flexibles (flexible images)
- Media queries

**¿Por qué es crítico?**
- Más de 60% del tráfico web es móvil
- Google usa mobile-first indexing
- Mejor experiencia de usuario
- Menor costo que mantener sitios separados
- SEO mejorado

---

## 3.5.1. Diseño Adaptable vs. Responsivo

### Responsive Design (Diseño Responsivo)

**Características:**
- Layout fluido que se adapta continuamente
- Usa porcentajes, ems, rems en vez de píxeles fijos
- Media queries para ajustes en breakpoints
- Un solo código base

**Ejemplo:**

```css
.container {
  width: 90%;  /* Fluido */
  max-width: 1200px;
  margin: 0 auto;
}

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 24px;
}

/* Se adapta continuamente, sin "saltos" bruscos */
```

**Ventajas:**
- ✓ Experiencia fluida en todos los tamaños
- ✓ Menos código (no necesita layouts específicos)
- ✓ Se adapta a dispositivos futuros
- ✓ Mantenimiento más simple

**Desventajas:**
- ✗ Puede ser complejo para layouts muy diferentes
- ✗ Requiere planificación cuidadosa
- ✗ Testing en múltiples tamaños

### Adaptive Design (Diseño Adaptable)

**Características:**
- Layouts específicos para tamaños específicos
- "Salta" entre layouts en breakpoints
- Puede servir código diferente según dispositivo

**Ejemplo:**

```css
/* Mobile: 1 columna */
@media (max-width: 767px) {
  .layout {
    grid-template-columns: 1fr;
  }
}

/* Tablet: 2 columnas */
@media (min-width: 768px) and (max-width: 1023px) {
  .layout {
    grid-template-columns: 1fr 1fr;
  }
}

/* Desktop: 3 columnas */
@media (min-width: 1024px) {
  .layout {
    grid-template-columns: 1fr 1fr 1fr;
  }
}
```

**Ventajas:**
- ✓ Control preciso en cada breakpoint
- ✓ Puede optimizar para dispositivos específicos
- ✓ Más fácil retroadaptar sitios existentes

**Desventajas:**
- ✗ Más código y complejidad
- ✗ "Saltos" visibles entre breakpoints
- ✗ Necesita actualización para nuevos dispositivos
- ✗ Mantenimiento más costoso

### Comparación

| Aspecto | Responsive | Adaptive |
|---------|-----------|----------|
| **Fluidez** | Continua | Discreta |
| **Breakpoints** | Guías, no límites | Límites estrictos |
| **Código** | Menos, más genérico | Más, más específico |
| **Control** | Menos preciso | Muy preciso |
| **Mantenimiento** | Más fácil | Más difícil |
| **Recomendado para** | Mayoría de casos | Casos muy específicos |

---

## 3.5.2. Mobile First vs. Desktop First

### Mobile First

**Filosofía:** Diseñar primero para mobile, luego expandir para pantallas más grandes.

**Ventajas:**
- ✓ Prioriza contenido esencial (constraints creativos)
- ✓ Mejor performance en móvil (carga lo mínimo)
- ✓ Alineado con mobile-first indexing de Google
- ✓ Progressive enhancement

**CSS Mobile First:**

```css
/* Base: Mobile (sin media query) */
.container {
  padding: 16px;
}

.grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
}

.sidebar {
  display: none;  /* Oculto en mobile */
}

/* Tablet y más grande */
@media (min-width: 768px) {
  .container {
    padding: 24px;
  }
  
  .grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
}

/* Desktop */
@media (min-width: 1024px) {
  .container {
    padding: 32px;
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }
  
  .sidebar {
    display: block;  /* Mostrar sidebar en desktop */
  }
}
```

**Proceso de diseño Mobile First:**

1. **Contenido y estructura primero**
   - ¿Qué es absolutamente esencial?
   - Priorizar en espacio limitado

2. **Diseñar para mobile (320-375px)**
   - Layout simple, 1 columna
   - Navegación simplificada
   - CTAs claros y grandes

3. **Expandir a tablet (768px)**
   - Agregar segunda columna si aplica
   - Mostrar más contenido

4. **Expandir a desktop (1024px+)**
   - Layouts complejos
   - Sidebars, multi-columna
   - Hover states, tooltips

### Desktop First

**Filosofía:** Diseñar primero para desktop, luego adaptar a mobile.

**Desventajas:**
- ✗ Tendencia a "esconder" contenido en mobile
- ✗ Puede resultar en mobile lento (cargar todo, ocultar)
- ✗ Graceful degradation (perder features)

**CSS Desktop First:**

```css
/* Base: Desktop */
.container {
  padding: 32px;
  max-width: 1200px;
  margin: 0 auto;
}

.grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
}

.sidebar {
  display: block;
}

/* Tablet y más pequeño */
@media (max-width: 1023px) {
  .grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }
}

/* Mobile */
@media (max-width: 767px) {
  .container {
    padding: 16px;
  }
  
  .grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }
  
  .sidebar {
    display: none;
  }
}
```

**Cuándo usar Desktop First:**
- Audiencia principalmente desktop (dashboards corporativos)
- Producto complejo que no funciona bien en mobile
- Retroadaptar sitio legacy

---

## 3.5.3. Breakpoints Estratégicos

### Breakpoints Comunes

```css
/* Basados en dispositivos comunes */

/* Extra Small (Móviles en portrait) */
@media (min-width: 320px) { }

/* Small (Móviles grandes / portrait) */
@media (min-width: 576px) { }

/* Medium (Tablets / portrait) */
@media (min-width: 768px) { }

/* Large (Tablets landscape / Laptops pequeños) */
@media (min-width: 1024px) { }

/* Extra Large (Laptops / Desktops) */
@media (min-width: 1280px) { }

/* 2X Large (Monitores grandes) */
@media (min-width: 1536px) { }
```

### Breakpoints de EduConnect

```css
:root {
  --breakpoint-sm: 640px;
  --breakpoint-md: 768px;
  --breakpoint-lg: 1024px;
  --breakpoint-xl: 1280px;
  --breakpoint-2xl: 1536px;
}

/* Mobile First */
.course-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
}

@media (min-width: 640px) {
  .course-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }
}

@media (min-width: 1024px) {
  .course-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }
}

@media (min-width: 1536px) {
  .course-grid {
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
  }
}
```

### Breakpoints Basados en Contenido

En vez de dispositivos, basar en cuando el diseño "se rompe":

```css
/* En vez de breakpoint arbitrario */
@media (min-width: 768px) { }

/* Usar cuando contenido necesita más espacio */
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 24px;
}

/* Auto-responsivo: crea columnas cuando caben */
```

### Tipos de Media Queries

**Width-based (más común):**

```css
@media (min-width: 768px) {
  /* Tablet y más grande */
}

@media (max-width: 767px) {
  /* Mobile y más pequeño */
}

@media (min-width: 768px) and (max-width: 1023px) {
  /* Solo tablet */
}
```

**Orientation:**

```css
@media (orientation: portrait) {
  /* Dispositivo en vertical */
}

@media (orientation: landscape) {
  /* Dispositivo en horizontal */
}
```

**Resolution/Pixel Density:**

```css
@media (min-resolution: 2dppx) {
  /* Pantallas retina/high-DPI */
  .logo {
    background-image: url('logo@2x.png');
  }
}
```

**Hover capability:**

```css
@media (hover: hover) {
  /* Dispositivo con mouse */
  .button:hover {
    background: var(--primary-dark);
  }
}

@media (hover: none) {
  /* Touch device */
  .button:active {
    background: var(--primary-dark);
  }
}
```

**Prefer-color-scheme:**

```css
@media (prefers-color-scheme: dark) {
  :root {
    --bg: #1a1a1a;
    --text: #ffffff;
  }
}
```

---

## 3.5.4. Imágenes y Medios Responsivos

### Imágenes Flexibles

**Básico:**

```css
img {
  max-width: 100%;
  height: auto;
}
```

### Picture Element

Servir diferentes imágenes según viewport:

```html
<picture>
  <!-- Mobile: Imagen cuadrada -->
  <source 
    media="(max-width: 767px)"
    srcset="hero-mobile.jpg"
  >
  
  <!-- Tablet: Imagen 4:3 -->
  <source 
    media="(max-width: 1023px)"
    srcset="hero-tablet.jpg"
  >
  
  <!-- Desktop: Imagen wide 16:9 -->
  <source 
    media="(min-width: 1024px)"
    srcset="hero-desktop.jpg"
  >
  
  <!-- Fallback -->
  <img src="hero-desktop.jpg" alt="Hero image">
</picture>
```

### Srcset y Sizes

Servir diferentes resoluciones según densidad de pantalla:

```html
<img
  src="course-400.jpg"
  srcset="
    course-400.jpg 400w,
    course-800.jpg 800w,
    course-1200.jpg 1200w
  "
  sizes="
    (max-width: 767px) 100vw,
    (max-width: 1023px) 50vw,
    33vw
  "
  alt="Curso de UX"
>
```

**Explicación:**
- `srcset`: Lista de imágenes disponibles con su ancho
- `sizes`: Cuánto espacio ocupará la imagen en diferentes viewports
- Navegador elige la imagen óptima

### Videos Responsivos

**Aspect Ratio Container:**

```html
<div class="video-container">
  <iframe
    src="https://www.youtube.com/embed/..."
    frameborder="0"
    allowfullscreen
  ></iframe>
</div>
```

```css
.video-container {
  position: relative;
  width: 100%;
  padding-bottom: 56.25%;  /* 16:9 aspect ratio */
  height: 0;
  overflow: hidden;
}

.video-container iframe {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}
```

**CSS aspect-ratio (moderno):**

```css
.video-container {
  aspect-ratio: 16 / 9;
  width: 100%;
}

.video-container iframe {
  width: 100%;
  height: 100%;
}
```

### Background Images Responsivas

```css
.hero {
  background-image: url('hero-mobile.jpg');
  background-size: cover;
  background-position: center;
}

@media (min-width: 768px) {
  .hero {
    background-image: url('hero-tablet.jpg');
  }
}

@media (min-width: 1024px) {
  .hero {
    background-image: url('hero-desktop.jpg');
  }
}

/* Retina */
@media 
  (min-width: 1024px) and 
  (min-resolution: 2dppx) {
  .hero {
    background-image: url('hero-desktop@2x.jpg');
  }
}
```

---

## 3.5.5. Prototipado Responsive

### Herramientas

**Figma:**

```
Artboards recomendados:
- Mobile: 375px (iPhone)
- Tablet: 768px (iPad portrait)
- Desktop: 1440px (Laptop estándar)
- Large Desktop: 1920px (Monitor)
```

**Frames responsivos en Figma:**
- Usar constraints (top, left, scale, etc.)
- Auto-layout para componentes fluidos
- Variants para versiones mobile/desktop

### Testing Responsive

**Navegador DevTools:**

```
Chrome DevTools:
1. F12 o Ctrl+Shift+I
2. Toggle device toolbar (Ctrl+Shift+M)
3. Seleccionar dispositivo o custom size
4. Testing de touch events, network throttling
```

**Dispositivos reales:**
- Imprescindible testear en dispositivos reales
- Emuladores no capturan todo (touch, performance)

**Servicios de testing:**
- BrowserStack
- LambdaTest
- Sauce Labs

### Consideraciones de Prototipado

**1. Touch vs. Mouse:**

```css
/* Áreas clickeables más grandes en touch */
@media (pointer: coarse) {
  .button {
    min-height: 44px;  /* WCAG 2.5.5 */
    padding: 12px 24px;
  }
  
  .link {
    padding: 8px 0;  /* Más espacio vertical */
  }
}
```

**2. Hover States:**

```css
/* Solo aplicar hover en dispositivos con mouse */
@media (hover: hover) {
  .card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 24px rgba(0,0,0,0.15);
  }
}

/* Touch: usar active en vez de hover */
@media (hover: none) {
  .card:active {
    transform: scale(0.98);
  }
}
```

**3. Navegación:**

```html
<!-- Desktop: Top nav -->
<nav class="desktop-nav" aria-label="Main navigation">
  <a href="/">Inicio</a>
  <a href="/cursos">Cursos</a>
  <a href="/categorias">Categorías</a>
</nav>

<!-- Mobile: Hamburger + Bottom tabs -->
<button class="hamburger" aria-label="Open menu">
  ☰
</button>

<nav class="bottom-tabs" aria-label="Main navigation">
  <a href="/">🏠 Inicio</a>
  <a href="/buscar">🔍 Buscar</a>
  <a href="/mis-cursos">📚 Cursos</a>
  <a href="/perfil">👤 Perfil</a>
</nav>
```

---

## Aplicación en EduConnect

### Homepage Responsive

**Mobile (< 768px):**
```
┌──────────────────┐
│ [☰]  Logo  [👤]  │
├──────────────────┤
│                  │
│  Hero (full)     │
│                  │
├──────────────────┤
│  Buscar cursos   │
├──────────────────┤
│ Curso Card       │
│ Curso Card       │
│ Curso Card       │
├──────────────────┤
│ Tab Bar          │
└──────────────────┘
```

**Tablet (768-1023px):**
```
┌─────────────────────────┐
│ Logo   Nav   [Sesión]   │
├─────────────────────────┤
│                         │
│  Hero (16:9)            │
│                         │
├─────────────────────────┤
│  Buscar cursos          │
├─────────────────────────┤
│ Curso   │  Curso        │
│ Card    │  Card         │
├─────────┼───────────────┤
│ Curso   │  Curso        │
│ Card    │  Card         │
└─────────────────────────┘
```

**Desktop (> 1024px):**
```
┌────────────────────────────────────┐
│ Logo   Nav Links   [Sesión][Sign]  │
├────────────────────────────────────┤
│  Hero │                            │
│  Text │  Hero Image (16:9)         │
│  CTA  │                            │
├───────┴────────────────────────────┤
│  Curso  │  Curso  │  Curso         │
│  Card   │  Card   │  Card          │
├─────────┼─────────┼────────────────┤
│  Curso  │  Curso  │  Curso         │
│  Card   │  Card   │  Card          │
└─────────┴─────────┴────────────────┘
```

### Código Responsive Completo

```css
/* Base: Mobile */
.homepage {
  padding: 16px;
}

.hero {
  display: flex;
  flex-direction: column;
  gap: 24px;
  padding: 32px 0;
}

.hero-content {
  order: 2;
}

.hero-image {
  order: 1;
  width: 100%;
  aspect-ratio: 1 / 1;
}

.course-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
  margin-top: 48px;
}

/* Tablet */
@media (min-width: 768px) {
  .homepage {
    padding: 24px;
  }
  
  .hero {
    padding: 64px 0;
  }
  
  .hero-image {
    aspect-ratio: 16 / 9;
  }
  
  .course-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
    margin-top: 64px;
  }
}

/* Desktop */
@media (min-width: 1024px) {
  .homepage {
    padding: 32px;
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .hero {
    flex-direction: row;
    align-items: center;
    padding: 96px 0;
  }
  
  .hero-content {
    flex: 1;
    order: 1;
  }
  
  .hero-image {
    flex: 1;
    order: 2;
  }
  
  .course-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
    margin-top: 96px;
  }
}

/* Large Desktop */
@media (min-width: 1536px) {
  .course-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}
```

---

## Conclusiones Clave

- **Mobile First:** Diseñar para constraints crea mejores experiencias
- **Content-based breakpoints:** Basar en contenido, no dispositivos
- **Fluid layouts:** Usar auto-fit, minmax para grids responsivos
- **Flexible media:** Imágenes y videos deben adaptarse
- **Testing real:** Testear en dispositivos reales es imprescindible
- **Touch-friendly:** 44px mínimo para targets en touch
- **Performance:** Móvil primero = mejor performance

---

## Referencias y Recursos

### Lecturas Recomendadas

1. **"Responsive Web Design" (2011)** - Ethan Marcotte
   - Libro que introdujo el concepto
   - Fundamentos de RWD

2. **"Mobile First" (2011)** - Luke Wroblewski
   - Por qué diseñar mobile-first
   - Casos de estudio

3. **"Responsible Responsive Design" (2015)** - Scott Jehl
   - Performance en responsive
   - Técnicas avanzadas

### Recursos en Línea

1. **This Is Responsive** (bradfrost.github.io/this-is-responsive)
   - Patrones responsive
   - Recursos y ejemplos

2. **Responsive Images Community Group** (responsiveimages.org)
   - Guías de picture, srcset
   - Casos de uso

3. **Can I Use** (caniuse.com)
   - Soporte de navegadores
   - Para media queries modernas

4. **Am I Responsive** (ami.responsivedesign.is)
   - Previsualizar sitio en múltiples dispositivos
   - Screenshots para presentaciones
